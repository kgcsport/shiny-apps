# apps/_shared/R/google_backup.R
# Stubs — implement later using googledrive / googlesheets4.

backup_to_google <- function(con = NULL) {
  invisible(NULL)
}

restore_from_google <- function(con = NULL) {
  invisible(NULL)
}
